To build the Omni-Shield and secure 1st place, you must execute a "Full-Stack Agentic" architecture. You aren't just building a frontend; you are building an autonomous loop: Detect (Envio) → Decide (AI/Logic) → Act (ERC-7715).Here is your exact execution blueprint and all the sources you need.### 1. The Core Stack & Source LinksComponentTechnologyOfficial Source / Starter LinkWallet BackboneMetaMask FlaskInstall Flask (Required for ERC-7715)Permissions SDKSmart Accounts KitGator Starter Template (GitHub)Account LogicEIP-7702ZeroDev 7702 GuideIndexing/EyesEnvio HyperSyncEnvio HyperSync DocsGasless InfrastructurePimlicoPimlico Dashboard (Get Bundler/Paymaster API Key)### 2. Step-by-Step Execution PlanStep 1: Bootstrap with the "Gator" TemplateDon't write the permission handshake from scratch. Use the official CLI to generate a working ERC-7715 app in 30 seconds.Command: npx create-gator-app@latestAction: Choose the "Experimental: Basic Gator app". This gives you the GrantPermissionsButton.tsx and the CreateSessionAccount.tsx pre-configured.Step 2: Define the "Shield" Permission (ERC-7715)In your code, you need to request the ability to act on the user's behalf. Unlike the competition who requests "spend" access, you will request "Approval Management" access.Code Location: src/components/GrantPermissionsButton.tsxThe Logic: Set a caveat that only allows the agent to call the approve(spender, 0) function. This makes it a "Revoke-Only" key, which is highly secure and will impress the judges.Step 3: Build the "Eyes" with Envio HyperSyncThis is how you "detect" the hack. You need a background script that monitors the blockchain for suspicious activity.Logic:Use Envio to index Approval events on Sepolia.If the script sees a "Transfer" event from a contract that is currently being flagged for a "Drain" (you can simulate this by monitoring high-volume outflows), it triggers your agent.Code Snippet:TypeScriptconst client = HypersyncClient.new({ url: "https://sepolia.hypersync.xyz" });
// Query for Approval events of the user
const stream = await client.stream(query, {});
// When a threat is detected:
if (threatDetected) {
   await executeEmergencyRevoke(sessionKey, permissionContext);
}
Step 4: Execute the "Auto-Revoke" (Redemption)Once Envio detects the threat, your app uses the permissionContext (returned by MetaMask) to send a gasless transaction via Pimlico.Action: The session account signs a UserOperation that calls token.approve(attacker_address, 0).Result: The attacker is "un-approved" before they can drain the wallet.### 3. How to "Brutally" Beat the Others (Submission Strategy)To ensure the win, your GitHub README and Submission Form must highlight these three "unfair advantages":"Autonomous Response" (The Kill Shot): Emphasize that your app works while the user is offline. Most competitors (#6, #9) require the user to be at their computer. You are building a "Smart Firewall.""EIP-7702 Synergy": Explicitly mention that you are using 7702 to "upgrade" an existing MetaMask wallet into a Smart Account temporarily. This is the "hottest" tech in the hackathon."The Envio Advantage": Claim the Best Use of Envio track by explaining that you use HyperSync's low-latency streaming to detect hacks in sub-second time—faster than any RPC-based tool.### 4. Final Submission Checklist[ ] Video: Record a demo where you "attack" a wallet and the Omni-Shield agent stops it automatically.[ ] Documentation: Include a "Technical Architecture" diagram showing the loop: Chain -> Envio -> AI Agent -> ERC-7715 -> Pimlico -> Chain.[ ] Social Media: Use your project to win the $100 social prize. Post a video of the "Auto-Revoke" on X and tag @MetaMaskDev.